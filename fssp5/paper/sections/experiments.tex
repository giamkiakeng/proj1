%% ---------------------------------------------------------------------
\section{Four states: a certified proof}\label{sec:four}

All formulas are produced by the generator \texttt{gencnf.py}; models are
decoded into rule tables and re-simulated by the independent brute-force
checker \texttt{fsspcheck.c}. Solvers: Kissat~4.0.4 \cite{Kissat20} and
CaDiCaL~3.0.1 \cite{CaDiCaL2} (or CaDiCaL~1.9.5 through PySAT
\cite{PySAT18} for incremental runs). Times are single-core wall-clock times
on a cloud virtual CPU.

\begin{theorem}\label{thm:four}
No rule with four states $\{\mathsf L,\mathsf G,a,\mathsf F\}$ satisfying
\eqref{eq:quiescence} synchronizes all lines of lengths $2,\dots,9$ in
minimal time. In particular there is no four-state minimal-time solution,
even without the condition $\delta(\ast,\mathsf L,\mathsf L)=\mathsf L$.
\end{theorem}

\begin{proof}
By \cref{lem:agree,lem:return} and \eqref{eq:neverfire} every such rule
yields a model of $\mathrm{MT}_4(\{2,\dots,9\};16)$ (for $k=4$ there is a
single auxiliary state and no symmetry breaking). CaDiCaL proves this
formula unsatisfiable in $51$\,s and emits an LRAT proof of $177$\,MB
($41$\,MB compressed), which \texttt{lrat-check} of the \textsf{drat-trim}
suite \cite{WetzlerHeuleHunt14} verifies in $5.7$\,s. The SHA-256 digests of
the formula and of the proof are listed in \ref{app:repro}.
\end{proof}

The bound $9$ is sharp for the plain formula: $\mathrm{MT}_4(\{2,\dots,8\};14)$
is satisfiable, and an independent depth-first enumeration (program
\texttt{fsspdfs.c}, which fills the table lazily in the order in which
transitions are needed) finds the numbers of partial rules listed in
\cref{tab:four}, counted on the transitions they use; it reaches zero exactly
at the lengths $2,\dots,9$, in agreement with the solver. With the half-line extended to
anti-diagonal $78$ the lengths $2,\dots,8$ already suffice. The pumping
constraints alone are weak for four states: together with the half-line up
to anti-diagonal~$78$, the lengths $2,\dots,7$ remain satisfiable.

\begin{table}[t]
\centering\small
\begin{tabular}{@{}lrrrrr@{}}
\toprule
largest length $N$ & 5 & 6 & 7 & 8 & 9\\
\midrule
partial rules synchronizing $2,\dots,N$ & 828{,}670 & 64{,}299 & 536 & 27 & 0\\
\bottomrule
\end{tabular}
\caption{Four states: number of partial rules (restricted to the transitions
they use) that synchronize every line of length $2,\dots,N$ in minimal time,
by exhaustive enumeration ($1.09\cdot10^{8}$ search nodes).}\label{tab:four}
\end{table}

%% ---------------------------------------------------------------------
\section{Five states: the frontier}\label{sec:five}

\subsection{Lucky rules}

For five states the table has $94$ free transitions with five values each,
against $45$ with four values for four states: the search space grows by a
factor of about $10^{38}$. \Cref{tab:five} lists representative instances.
All of them are satisfiable, and in each case the decoded rule fails for
some length that was not encoded; we call such rules \emph{lucky}. Two kinds
occur. Rules of the first kind have a \emph{periodic} half-line behind the
front (\cref{fig:diagrams}(b)); they can even synchronize an isolated long
line such as $n=20$, but they violate the pumping lemma massively (155 of the
pairs $4\le n<n'\le40$ for the rule shown). Rules of the second kind have a
chaotic half-line that eventually fires. Once the half-line is extended and
the pumping constraints are imposed, the solver is forced to produce
half-lines with a genuine slow boundary: in the
models we examined in detail---\cref{fig:diagrams}(c) and the rule of
\cref{thm:frontier}, whose half-line consists of a front of $\mathsf G$'s, a
solid wake of $\mathsf G$'s and a region $\mathsf{A}\,\mathsf{L}\,\mathsf{A}\,\mathsf{L}\cdots$
behind a boundary of speed exactly $1/3$---a region delimited by a line of
slope $1/3$ appears, as predicted by \cref{thm:third}.

\begin{table}[t]
\centering\small
\begin{tabular}{@{}p{0.52\textwidth}p{0.28\textwidth}r@{}}
\toprule
instance ($k=5$) & result & time\\
\midrule
$\mathrm{MT}_5(\{2,\dots,10\};18)$ & SAT, fails at $n=11$ & $10.8$\,s\\
$\mathrm{MT}_5(\{2,\dots,12\};22)$ & SAT, fails at $n=13$ & $\approx 20$\,min\\
$\mathrm{MT}_5(\{2,\dots,9,16\};30)$ & SAT, fails at $n=10$ & $23.6$\,s\\
$\mathrm{MT}_5(\{2,\dots,9,20\};38)$ & SAT, fails at $n=10$ & $82.4$\,s\\
$\mathrm{MT}_5(\{2,\dots,8\};78,40)$ & SAT, fails at $n=9$ & $68.7$\,s\\
$\mathrm{MT}_5(\{2,\dots,10\};78,40)$ & SAT, fails at $n=11$ & $755.6$\,s\\
$\mathrm{MT}_5(\{2,\dots,12\};78,40)$ + links + uniform pre-firing $\mathsf A^n$ & SAT, fails at $n=13$ & $1930$\,s\\
\bottomrule
\end{tabular}
\caption{Five-state instances. $\mathrm{MT}_k(\mathcal N;M,N')$: lengths
$\mathcal N$, half-line up to anti-diagonal $M$, pumping pairs up to $N'$.}
\label{tab:five}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{fig_diagrams.pdf}
\caption{Space-time diagrams, time downwards. (a) Mazoyer's six-state
solution on $n=40$ cells; the last row is the firing row. (b) Half-line of a
five-state rule that synchronizes the lengths $2,\dots,9$ and $20$ but
violates the pumping lemma: its background is periodic. (c) Half-line of a
five-state rule found under the pumping constraints: the boundary between
the checkered region and the diagonal background has slope $1/3$.}
\label{fig:diagrams}
\end{figure}

\subsection{A five-state rule for all lengths up to twelve}

\begin{theorem}\label{thm:frontier}
The five-state rule $\delta_{12}$ of \cref{tab:delta12} has the following
properties.
\begin{enumerate}[label=(\alph*)]
\item It synchronizes the line of length $n$ at time $2n-2$ for every
  $2\le n\le 12$, and one step before firing all cells are in state
  $\mathsf A$.
\item Its half-line diagram contains no firing cell $(t,i)$ with
  $t+i\le 78$.
\item It satisfies $\mathrm{PUMP}(n,n')$ for all $4\le n<n'\le 40$.
\end{enumerate}
It does not synchronize the line of length $13$: a cell fires at time $23$.
\end{theorem}

\begin{proof}
Each property is a finite computation on the explicit table, carried out by
direct simulation: (a) and the failure at $n=13$ with the checker
\texttt{fsspcheck.c}, (b) with \texttt{check\_halfline.py}, and (c) with
\texttt{pumping.py}, which recomputes the cones of \cref{lem:cone} by
breadth-first search (\ref{app:repro}).
\end{proof}

Consequently, the necessary conditions used in this paper, restricted to
lengths at most $12$ (even together with the pumping constraints up to $40$
and the half-line up to anti-diagonal $78$), are satisfiable: a refutation
of five-state minimal-time solutions along these lines must involve lines of
length at least $13$, and the requirement ``uniform configuration before
firing'', satisfied by Mazoyer's solution, does not help below that length.

\subsection{Where the difficulty lies}

\paragraph{The half-line determines everything} Take any of the rules found
above, fix the transitions it uses on the half-line up to anti-diagonal $78$
(between $39$ and $50$ transitions) and ask whether the remaining transitions
can be completed so that all lengths $2,\dots,N$ synchronize. For the
five-state germs we tried, Kissat decides this in less than one second: for
the rule of \cref{tab:five} with lengths $2,\dots,10$ the completion exists
for $N=8$ and not for $N=12$; the other germs fail already for $N=8$.
As a sanity check on a genuine solution, fixing the $58$ transitions that
Mazoyer's rule uses on its half-line up to anti-diagonal $78$ leaves a
completion problem for the lengths $3,\dots,12$ that Kissat solves in $4$\,s. Conversely, the unsatisfiable core
returned for such a germ contains $38$ of its $39$ transitions, so that
learning from the completion problem (a logic-based Benders scheme we
implemented) does not prune the half-line search effectively.

\paragraph{Size of the search} We estimated the size of the natural
look-ahead search tree---half-line transitions fixed in order of first use,
anti-diagonal by anti-diagonal, a node being discarded when the full formula
with the partial table as assumptions is refuted within $3000$ conflicts---
with Knuth's random-probe estimator. For four states ($\mathrm{MT}_4$ with
lengths $2,\dots,9$) the mean over $30$ probes is $27$ nodes. For five
states ($\mathrm{MT}_5(\{2,\dots,12\};78,40)$ with links) three probes gave
$4.2\cdot10^{11}$, $3.1\cdot10^{17}$ and $8.3\cdot10^{18}$; adding the
lengths up to $18$ prunes more strongly along some paths but not in general
(three probes: $2.2\cdot10^{5}$, $4.2\cdot10^{12}$, $1.0\cdot10^{15}$). The estimator has
a large variance, but the gap of at least ten orders of magnitude is robust
and explains why the unrestricted five-state formulas with $N\ge12$ that we
started (up to $N=18$, several CPU-hours each) did not terminate. Clause
learning compresses such trees considerably, and cube-and-conquer
\cite{HeuleEtAl11} parallelizes them almost linearly, but a complete
five-state search along these lines is a computation of a much larger scale
than the ones reported here.
